type Todo @model @auth(rules: [
    { allow: owner, operations: [create, update, delete] },
    { allow: private, operations: [read] },
  ]) {
  id: ID!
  name: String!
  description: String
  isComplete: Boolean
}
